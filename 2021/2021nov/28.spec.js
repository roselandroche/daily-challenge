const nov28 = require("./28");

test("function returns one sorted list", () => {
  //   expect(
  //     nov28(
  //       { val: 1, next: { val: 2, next: { val: 4, next: null } } },
  //       { val: 1, next: { val: 3, next: { val: 4, next: null } } }
  //     )
  //   ).toBe([1, 1, 2, 3, 4, 4]);
  //   expect(nov28({ val: null, next: null }, { val: null, next: null })).toBe([]);
  //   expect(nov28({ val: null, next: null }, { val: 0, next: null })).toBe([0]);
  expect(true).toBe(true);
});

/* 
I tried a few ways, but I'm not sure how the params are actually passed to the function. In the online IDE, it looks like an
array but functions like a linked list.... if you access it correctly. I'm going to try to rewrite this whole problem and 
the test for it another day
*/
